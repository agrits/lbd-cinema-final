zuul:
  ignoredServices: '*'
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 20000

  routes:
<<<<<<< HEAD
#    user-service:
#      path: /uaa/** #path that will be intercepted
#      url: user-service #which url the request will e mapped to
#      stripPrefix: false #if zuul should replace the "/uaa/" segment
#      sensitiveHeaders:
#
#    account-service:
#      path: /accounts/**
#      serviceId: account-service
#      stripPrefix: false
#      sensitiveHeaders:
=======
    user-service:
      path: /api/user/**
      url: user-service
      stripPrefix: false
      sensitiveHeaders:

    cinema-service:
      path: /api/cinema/**
      serviceId: cinema-service
      stripPrefix: false
      sensitiveHeaders:

    reservation-service:
      path: /api/reservation/**
      serviceId: reservation-service
      stripPrefix: false
      sensitiveHeaders:

    show-service:
      path: /api/show/**
      serviceId: show-service
      stripPrefix: false
      sensitiveHeaders:

    movie-service:
      path: /api/movie/**
      serviceId: movie-service
      stripPrefix: false
      sensitiveHeaders:

    movie-service:
      path: /movie_api/**        #path that will be intercepted
      url: movie-service    #which url the request will e mapped to
      stripPrefix: false    #if zuul should replace the "/uaa/" segment
      sensitiveHeaders:

    show-service:
      path: /show_api/**        #path that will be intercepted
      url: show-service    #which url the request will e mapped to
      stripPrefix: false    #if zuul should replace the "/uaa/" segment
      sensitiveHeaders:


>>>>>>> b9c0ffc6923e1015e9afe087255ea5ce51924e9c

    reservation-service:
      path: /service/reservation/**
      url: reservation-service
      stripPrefix: false
      sensitiveHeaders:

server:
  port: 8080